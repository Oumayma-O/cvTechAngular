import { AfterDelayDirective } from './after-delay.directive';

describe('AfterDelayDirective', () => {
  it('should create an instance', () => {
    // const directive = new AfterDelayDirective();
    expect(true).toBeTruthy();
  });
});
